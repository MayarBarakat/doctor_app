import 'package:flutter/material.dart';

class RejectedOperations extends StatelessWidget {
  const RejectedOperations({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
